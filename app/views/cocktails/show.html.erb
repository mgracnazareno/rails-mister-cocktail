<%= render "pages/header" %>

<div class="container">
  <div class="row">
    <%= @cocktail.name %>
  </div>

  <div class="container-card">
      <div class="row">
        <% @cocktails.doses.each |dose| %>
          <%= dose.description %>
          <%= dose.ingredient %>
        <% end %>
      </div>
    </div>

   <%= link_to "Add dose", new_cocktail_path(@cocktail)%>
</div>
